spring.application.name=choreboard-backend
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/choreboard}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Rylan2014!}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# for security to be able to test endpoints with postman
spring.security.user.name=${SECURITY_USER:user}
spring.security.user.password=${SECURITY_PASSWORD:password}